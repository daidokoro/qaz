# Global Values - [Required]
region: "eu-central-1"
project: "example-dev"

# Stack values
stacks:
  # Define sqs stack here
  sqs:
    cf:
      Queues:
        - QueueName: "daido"
        - QueueName: "koro"
      indexdocument: "main.html"
